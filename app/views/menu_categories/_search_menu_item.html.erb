<section class="Section">
  <% if @menu_items && @menu_items.size > 0 %>
    <%= render partial: "menu_list_section", locals: { menu_items: @menu_items } %>
  <% else %>
    <p>No results found!</p>
  <% end %>
  </section>
